global:
  scrape_interval: 5s

scrape_configs:
  - job_name: prometheus
    static_configs:
      - targets: ["localhost:9090"]

  # The app exposes /metrics on :9000 once `arb record` lands.
  - job_name: arb
    static_configs:
      - targets: ["app:9000"]
