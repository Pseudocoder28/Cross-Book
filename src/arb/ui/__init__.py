"""Live market-data terminal UI: FastAPI backend plus static assets.

Read-only, like everything on day 1 — the UI observes the same normalized
books the recorder pipeline produces; it places no orders.
"""
